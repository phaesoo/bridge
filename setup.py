from setuptools import setup


setup(
    name='bridge',
    version='1.0.0',
    author='hspark',
    packages=['bridge'],
    include_package_data=True,
    install_requires=[
        'flask',
    ],
)
